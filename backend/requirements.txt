# --- Web / API layer ---
Django==5.2.1
djangorestframework==3.16.0
django-cors-headers==4.3.1
gunicorn==22.0.0
psycopg2-binary==2.9.9

# --- Async task layer ---
celery==5.4.0
redis==5.0.4

# --- Distributed processing layer ---
pyspark==3.5.1

# --- File ingest + paged result reads ---
pandas==2.2.2
openpyxl==3.1.2
pyarrow==16.1.0
duckdb==1.0.0

# --- Object storage (only used when STORAGE_BACKEND=s3) ---
# boto3 handles uploads/downloads; Spark reaches S3 via the hadoop-aws jars
# added in the Dockerfile, and DuckDB via its bundled httpfs extension.
boto3==1.34.145

# --- LLM integration ---
# 0.49 predates structured outputs (output_config.format) and the current
# models; 0.113 is the SDK the NL->regex path is written against.
anthropic==0.113.0

# --- Observability (optional; `--profile observability`) ---
flower==2.0.1

# --- Tests ---
pytest==8.2.2
pytest-django==4.8.0

# --- Type stubs (dev only; make pyright/Pylance see Django ORM managers,
# DRF Request.query_params, and Celery task .delay/.apply_async) ---
django-stubs==6.0.6
djangorestframework-stubs==3.17.0
celery-types==0.26.0
